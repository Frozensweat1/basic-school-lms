<div class="space-y-6">
<div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">Administration</p><h2 class="mt-2 text-2xl font-bold">Users</h2><p class="mt-1 text-sm text-slate-600">Manage LMS accounts and roles.</p></div><x-button wire:click="create" target="create" :loading="true" icon="plus">Add user</x-button></div>
<input wire:model.live.debounce.400ms="search" placeholder="Search name or email…" class="w-full rounded-lg border-slate-300 sm:max-w-sm">
<div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50"><tr><th class="px-5 py-3 text-left">Name</th><th class="px-5 py-3 text-left">Email</th><th class="px-5 py-3 text-left">Role</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y divide-slate-200">@forelse($users as $user)<tr wire:key="user-{{ $user->id }}"><td class="px-5 py-4 font-medium">{{ $user->name }}</td><td class="px-5 py-4">{{ $user->email }}</td><td class="px-5 py-4">{{ $user->roles->pluck('name')->join(', ') ?: 'No role' }}</td><td class="px-5 py-4"><div class="flex justify-end gap-2"><x-ui.icon-button wire:click="edit({{ $user->id }})" icon="edit" label="Edit user" target="edit({{ $user->id }})" />@if($user->id !== auth()->id())<x-ui.icon-button wire:click="confirmDelete({{ $user->id }})" icon="trash" variant="danger" label="Delete user" target="confirmDelete({{ $user->id }})" />@endif</div></td></tr>@empty<tr><td colspan="4" class="px-5 py-12 text-center text-slate-500">No users found.</td></tr>@endforelse</tbody></table></div><x-pagination :paginator="$users" />
<x-modal :show="$showFormModal" :title="$editingId ? 'Edit user' : 'Add user'" close-action="closeModals"><form wire:submit="save" class="space-y-5"><input wire:model.blur="name" placeholder="Name" class="w-full rounded-lg border-slate-300">@error('name')<p class="text-sm text-rose-700">{{ $message }}</p>@enderror<input wire:model.blur="email" type="email" placeholder="Email" class="w-full rounded-lg border-slate-300">@error('email')<p class="text-sm text-rose-700">{{ $message }}</p>@enderror<input wire:model.blur="password" type="password" placeholder="{{ $editingId ? 'New password (optional)' : 'Password' }}" class="w-full rounded-lg border-slate-300"><select wire:model.blur="role" class="w-full rounded-lg border-slate-300"><option value="">Choose role</option>@foreach($roles as $role)<option value="{{ $role->name }}">{{ $role->name }}</option>@endforeach</select><div class="flex justify-end gap-3"><x-button type="button" wire:click="closeModals" variant="ghost">Cancel</x-button><x-button type="submit" icon="save" target="save">Save user</x-button></div></form></x-modal>
</div>

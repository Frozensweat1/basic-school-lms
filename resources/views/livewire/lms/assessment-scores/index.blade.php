<div class="space-y-6"><div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">Assessment scoring</p><h2 class="mt-2 text-2xl font-bold">{{ $assessment->title }}</h2><p class="text-sm text-slate-600">Maximum score: {{ $assessment->max_score }}</p></div><form wire:submit="save" class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase text-slate-500"><tr><th class="px-5 py-3">Student</th><th class="px-5 py-3">Score</th><th class="px-5 py-3">Grade</th><th class="px-5 py-3">Comment</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($students as $enrollment)<tr wire:key="assessment-student-{{ $enrollment->student_id }}"><td class="px-5 py-4">{{ $enrollment->student->first_name }} {{ $enrollment->student->last_name }}</td><td class="px-5 py-4"><input wire:model.blur="scores.{{ $enrollment->student_id }}" type="number" min="0" max="{{ $assessment->max_score }}" step=".01" class="w-28 rounded-lg border-slate-300"></td><td class="px-5 py-4 font-semibold">{{ $this->gradeFor($scores[$enrollment->student_id] ?? '') ?? '—' }}</td><td class="px-5 py-4"><input wire:model.blur="comments.{{ $enrollment->student_id }}" class="w-full rounded-lg border-slate-300"></td></tr>@endforeach</tbody></table><div class="border-t px-5 py-4 text-right"><x-button type="submit" icon="save" target="save">Save scores</x-button></div></form></div>

<div class="space-y-6"><div><p class="text-xs font-semibold uppercase tracking-widest text-slate-500">Website CMS</p><h1 class="text-2xl font-bold">Teacher spotlight</h1><p class="text-sm text-slate-600">Choose active teachers shown on the public teachers page.</p></div><div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">@foreach($teachers as $teacher)<article class="flex items-center gap-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex h-12 w-12 items-center justify-center rounded-full bg-blue-100 font-semibold text-blue-800">{{ strtoupper(substr($teacher->first_name,0,1).substr($teacher->last_name,0,1)) }}</div><div class="min-w-0 flex-1"><h2 class="font-semibold">{{ $teacher->first_name }} {{ $teacher->last_name }}</h2><p class="text-sm text-slate-500">{{ $teacher->email }}</p></div><button wire:click="toggle({{ $teacher->id }})" class="rounded-full px-3 py-1 text-xs font-semibold {{ $teacher->is_featured_on_website ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-600' }}">{{ $teacher->is_featured_on_website ? 'Featured' : 'Feature' }}</button></article>@endforeach</div></div>

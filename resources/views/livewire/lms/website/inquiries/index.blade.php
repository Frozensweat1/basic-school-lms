<div class="space-y-6"><div><p class="text-xs font-semibold uppercase tracking-widest text-slate-500">Website CMS</p><h1 class="text-2xl font-bold">Contact inquiries</h1></div><div class="space-y-3">@forelse($inquiries as $inquiry)<article class="rounded-2xl border {{ $inquiry->is_read ? 'border-slate-200' : 'border-blue-300 bg-blue-50/30' }} bg-white p-5 shadow-sm"><div class="flex items-start justify-between gap-4"><div><h2 class="font-semibold">{{ $inquiry->name }} <span class="font-normal text-slate-500">({{ $inquiry->email }})</span></h2><p class="mt-3 whitespace-pre-line text-sm text-slate-700">{{ $inquiry->message }}</p><p class="mt-3 text-xs text-slate-500">{{ $inquiry->created_at?->format('d M Y H:i') }}</p></div><div class="flex shrink-0 gap-3 text-sm">@if(!$inquiry->is_read)<button wire:click="markRead({{ $inquiry->id }})" class="text-blue-700">Mark read</button>@endif<button wire:click="delete({{ $inquiry->id }})" wire:confirm="Delete this inquiry?" class="text-red-600">Delete</button></div></div></article>@empty<div class="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-slate-500">No inquiries received.</div>@endforelse</div><x-pagination :paginator="$inquiries" /></div>

<div class="space-y-6"><div class="flex justify-between"><div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">Scheduling</p><h2 class="mt-2 text-2xl font-bold">{{ $timetable->name }}</h2></div><x-button wire:click="create" icon="plus" target="create">Add entry</x-button></div><div class="rounded-2xl border border-slate-200 bg-white"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase text-slate-500"><tr><th class="px-5 py-3">Day</th><th class="px-5 py-3">Period</th><th class="px-5 py-3">Class subject</th><th class="px-5 py-3">Teacher</th><th class="px-5 py-3">Room</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($entries as $entry)<tr wire:key="entry-{{ $entry->id }}"><td class="px-5 py-4">{{ ['','Monday','Tuesday','Wednesday','Thursday','Friday'][$entry->day_of_week] }}</td><td class="px-5 py-4">{{ $entry->schedulePeriod->name }}</td><td class="px-5 py-4">{{ $entry->schoolClass->name }} · {{ $entry->classSubject->subject->name }}</td><td class="px-5 py-4">{{ $entry->teacher?->first_name }} {{ $entry->teacher?->last_name }}</td><td class="px-5 py-4">{{ $entry->room }}</td><td class="px-5 py-4"><x-ui.icon-button wire:click="delete({{ $entry->id }})" icon="trash" label="Remove entry" variant="danger" target="delete({{ $entry->id }})" /></td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-slate-500">No entries added yet.</td></tr>@endforelse</tbody></table></div><x-modal :show="$showFormModal" title="Add timetable entry" close-action="closeModal"><form wire:submit="save" class="space-y-5"><div><label class="block text-sm font-medium">Class subject</label><select wire:model.blur="classSubjectId" class="mt-1 block w-full rounded-lg border-slate-300"><option value="">Choose</option>@foreach($classSubjects as $subject)<option value="{{ $subject->id }}">{{ $subject->schoolClass->name }} · {{ $subject->subject->name }}</option>@endforeach</select></div><div class="grid gap-5 sm:grid-cols-2"><div><label class="block text-sm font-medium">Day</label><select wire:model.blur="dayOfWeek" class="mt-1 block w-full rounded-lg border-slate-300">@foreach(['Monday','Tuesday','Wednesday','Thursday','Friday'] as $key=>$day)<option value="{{ $key+1 }}">{{ $day }}</option>@endforeach</select></div><div><label class="block text-sm font-medium">Period</label><select wire:model.blur="periodId" class="mt-1 block w-full rounded-lg border-slate-300"><option value="">Choose</option>@foreach($periods as $period)<option value="{{ $period->id }}">{{ $period->name }} ({{ $period->starts_at }}–{{ $period->ends_at }})</option>@endforeach</select>@error('periodId')<p class="text-sm text-rose-700">{{ $message }}</p>@enderror</div></div><div><label class="block text-sm font-medium">Room</label><input wire:model.blur="room" class="mt-1 block w-full rounded-lg border-slate-300"></div><div class="flex justify-end gap-3"><x-button type="button" wire:click="closeModal" variant="secondary">Cancel</x-button><x-button type="submit" icon="save" target="save">Add entry</x-button></div></form></x-modal></div>

<div class="space-y-6">
    <div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">School schedule</p><h2 class="mt-2 text-2xl font-bold text-slate-900">My timetable</h2></div>
    <div class="grid gap-3">@forelse($entries as $entry)<article class="rounded-2xl border border-slate-200 bg-white p-4"><div class="flex flex-wrap items-start justify-between gap-3"><div><p class="font-semibold text-slate-900">{{ $entry->classSubject->subject->name }}</p><p class="mt-1 text-sm text-slate-600">{{ ['','Monday','Tuesday','Wednesday','Thursday','Friday'][$entry->day_of_week] ?? 'Day '.$entry->day_of_week }} · {{ $entry->teacher->first_name }} {{ $entry->teacher->last_name }}</p></div><span class="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-semibold text-blue-700">{{ $entry->schedulePeriod->name ?? 'Scheduled period' }}</span></div><p class="mt-2 text-xs text-slate-500">{{ $entry->schedulePeriod?->starts_at }} – {{ $entry->schedulePeriod?->ends_at }} @if($entry->room) · Room {{ $entry->room }} @endif</p></article>@empty<div class="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-slate-500">No published timetable is available.</div>@endforelse</div>
</div>

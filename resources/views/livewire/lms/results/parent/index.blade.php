<div class="space-y-6"><div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">Academic results</p><h2 class="mt-2 text-2xl font-bold text-slate-900">Ward results</h2></div><select wire:model.live="studentId" class="block w-full max-w-sm rounded-lg border-slate-300"><option value="">Choose a ward</option>@foreach($students as $ward)<option value="{{ $ward->id }}">{{ $ward->first_name }} {{ $ward->last_name }}</option>@endforeach</select><div class="grid gap-3">@forelse($results as $result)<article class="rounded-2xl border border-slate-200 bg-white p-4"><p class="font-semibold text-slate-900">{{ $result->classSubject->subject->name }} · {{ $result->term->name }}</p><p class="mt-1 text-sm text-slate-600">Score {{ $result->total_score }} · Grade {{ $result->grade ?: '—' }}</p></article>@empty<div class="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-slate-500">No published results for this ward.</div>@endforelse</div></div>

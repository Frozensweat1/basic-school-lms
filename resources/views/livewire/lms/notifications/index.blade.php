<div class="space-y-6"><div class="flex justify-between"><div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">Communication</p><h2 class="mt-2 text-2xl font-bold">Notifications</h2></div><x-button wire:click="markAllRead" variant="secondary" target="markAllRead">Mark all read</x-button></div><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white">@forelse($notifications as $notification)<button wire:click="markRead('{{ $notification->id }}')" wire:key="notification-{{ $notification->id }}" class="block w-full border-b px-5 py-4 text-left hover:bg-slate-50 {{ $notification->read_at ? '' : 'bg-blue-50/50' }}"><p class="font-medium text-slate-900">{{ $notification->data['title'] ?? class_basename($notification->type) }}</p><p class="mt-1 text-sm text-slate-600">{{ $notification->data['message'] ?? 'You have a new notification.' }}</p><p class="mt-1 text-xs text-slate-500">{{ $notification->created_at->diffForHumans() }}</p></button>@empty<div class="p-10 text-center text-slate-500">No notifications yet.</div>@endforelse</div>{{ $notifications->links() }}</div>

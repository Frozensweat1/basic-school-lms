<div class="space-y-6"><div class="flex items-start justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">Communication</p><h2 class="mt-2 text-2xl font-bold">Notifications</h2><p class="mt-1 text-sm text-slate-600">Updates about learning, submissions, results, and school activity.</p></div>@if($unreadCount)<x-button wire:click="markAllRead" variant="secondary" target="markAllRead">Mark all read ({{ $unreadCount }})</x-button>@endif</div><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white">@forelse($notifications as $notification)@php($data=$notification->data ?? [])<div wire:key="notification-{{ $notification->id }}" class="flex items-start gap-4 border-b px-5 py-4 last:border-b-0 {{ $notification->read_at ? '' : 'bg-blue-50/50' }}"><div class="mt-1 h-2.5 w-2.5 shrink-0 rounded-full {{ $notification->read_at ? 'bg-slate-200' : 'bg-blue-600' }}"></div><div class="min-w-0 flex-1"><p class="font-medium text-slate-900">{{ $data['title'] ?? class_basename($notification->type) }}</p><p class="mt-1 text-sm text-slate-600">{{ $data['message'] ?? 'You have a new notification.' }}</p><div class="mt-2 flex flex-wrap items-center gap-3 text-xs text-slate-500"><span>{{ $notification->created_at->diffForHumans() }}</span>@if(!$notification->read_at)<button wire:click="markRead('{{ $notification->id }}')" wire:loading.attr="disabled" wire:target="markRead('{{ $notification->id }}')" class="font-semibold text-blue-700 hover:text-blue-900">Mark read</button>@endif @if(!empty($data['url']))<a href="{{ $data['url'] }}" class="font-semibold text-slate-700 hover:text-blue-700">Open</a>@endif</div></div></div>@empty<div class="p-10 text-center text-slate-500">No notifications yet.</div>@endforelse</div><x-pagination :paginator="$notifications" /></div>

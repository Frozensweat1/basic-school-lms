<div class="space-y-6"><div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach($metrics as $label=>$value)<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">{{ $label }}</p><p class="mt-3 text-2xl font-bold text-slate-900">{{ $value }}</p></div>@endforeach</div><div class="grid gap-6 xl:grid-cols-2"><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold text-slate-900">Recent results</h2><div class="mt-4 space-y-3">@forelse($recentResults as $result)<div class="flex items-center justify-between rounded-xl bg-slate-50 p-3"><span class="font-medium text-slate-900">{{ $result->assessment->classSubject->subject->name }} · {{ $result->assessment->title }}</span><span class="font-semibold text-blue-700">{{ $result->score }}</span></div>@empty<p class="text-sm text-slate-500">No results have been published yet.</p>@endforelse</div></section><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold text-slate-900">Announcements</h2><div class="mt-4 space-y-3">@forelse($announcements as $announcement)<div class="rounded-xl bg-slate-50 p-3"><p class="font-medium text-slate-900">{{ $announcement->title }}</p><p class="text-xs text-slate-500">{{ $announcement->published_at?->diffForHumans() }}</p></div>@empty<p class="text-sm text-slate-500">No announcements for your classes.</p>@endforelse</div></section></div></div>

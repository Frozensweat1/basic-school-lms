<div class="space-y-6"><div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach($metrics as $label=>$value)<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">{{ $label }}</p><p class="mt-3 text-3xl font-bold text-slate-900">{{ $value }}</p></div>@endforeach</div><div class="grid gap-6 xl:grid-cols-2"><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold text-slate-900">Recent submissions</h2><div class="mt-4 space-y-3">@forelse($recentSubmissions as $submission)<div class="flex items-center justify-between gap-3 rounded-xl bg-slate-50 p-3"><div><p class="font-medium text-slate-900">{{ $submission->student->first_name }} {{ $submission->student->last_name }}</p><p class="text-xs text-slate-500">{{ $submission->assignment->title }}</p></div><span class="text-xs text-slate-500">{{ $submission->submitted_at?->diffForHumans() }}</span></div>@empty<p class="text-sm text-slate-500">No submissions waiting for review.</p>@endforelse</div></section><section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="font-semibold text-slate-900">Your announcements</h2><div class="mt-4 space-y-3">@forelse($announcements as $announcement)<div class="rounded-xl bg-slate-50 p-3"><p class="font-medium text-slate-900">{{ $announcement->title }}</p><p class="text-xs text-slate-500">{{ $announcement->published_at?->diffForHumans() }}</p></div>@empty<p class="text-sm text-slate-500">No recent announcements.</p>@endforelse</div></section></div></div>

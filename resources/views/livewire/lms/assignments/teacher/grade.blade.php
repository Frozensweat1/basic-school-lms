<div class="space-y-6"><div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">Assignment grading</p><h2 class="mt-2 text-2xl font-bold text-slate-900">{{ $assignment->title }}</h2></div><div class="grid gap-4">@forelse($submissions as $submission)<article wire:key="submission-{{ $submission->id }}" class="rounded-2xl border border-slate-200 bg-white p-5"><h3 class="font-semibold text-slate-900">{{ $submission->student->first_name }} {{ $submission->student->last_name }}</h3><p class="mt-2 whitespace-pre-wrap text-sm text-slate-700">{{ $submission->submission_text }}</p>@if($submission->attachments->isNotEmpty())<div class="mt-3 flex flex-wrap gap-2"><span class="text-xs font-semibold uppercase tracking-wide text-slate-500">Attachments</span>@foreach($submission->attachments as $attachment)<button wire:click="downloadAttachment({{ $attachment->id }})" wire:loading.attr="disabled" wire:target="downloadAttachment({{ $attachment->id }})" class="rounded-lg bg-slate-100 px-3 py-2 text-xs font-semibold text-slate-700 hover:bg-slate-200">{{ $attachment->name }}</button>@endforeach</div>@endif<div class="mt-4 grid gap-4 sm:grid-cols-2"><div><label class="block text-sm font-medium text-slate-700">Score / {{ $assignment->max_score }}</label><input wire:model="scores.{{ $submission->id }}" type="number" min="0" max="{{ $assignment->max_score }}" step="0.01" class="mt-1 block w-full rounded-lg border-slate-300"></div><div><label class="block text-sm font-medium text-slate-700">Feedback</label><textarea wire:model="feedback.{{ $submission->id }}" rows="2" class="mt-1 block w-full rounded-lg border-slate-300"></textarea></div></div><x-button class="mt-4" wire:click="saveGrade({{ $submission->id }})" icon="save" target="saveGrade({{ $submission->id }})">Save grade</x-button></article>@empty<div class="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-slate-500">No submissions yet.</div>@endforelse</div></div>

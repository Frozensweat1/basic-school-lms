<div class="space-y-6"><div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">Learning progress</p><h2 class="mt-2 text-2xl font-bold text-slate-900">Ward assignments</h2><p class="mt-1 text-sm text-slate-600">Track submission status, scores, and teacher feedback.</p></div><select wire:model.live="studentId" class="block w-full max-w-sm rounded-lg border-slate-300"><option value="">Choose a ward</option>@foreach($students as $ward)<option value="{{ $ward->id }}">{{ $ward->first_name }} {{ $ward->last_name }}</option>@endforeach</select><div class="grid gap-4">@forelse($assignments as $assignment)<article wire:key="assignment-{{ $assignment->id }}" class="rounded-2xl border border-slate-200 bg-white p-5"><div class="flex flex-wrap items-start justify-between gap-4"><div><h3 class="font-semibold text-slate-900">{{ $assignment->title }}</h3><p class="mt-1 text-sm text-slate-500">{{ $assignment->classSubject->subject->name }} · Due {{ $assignment->due_at->format('d M Y, H:i') }}</p></div><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $assignment->submission?->status === 'graded' ? 'bg-emerald-100 text-emerald-700' : ($assignment->submission ? 'bg-blue-100 text-blue-700' : ($assignment->due_at->isPast() ? 'bg-rose-100 text-rose-700' : 'bg-amber-100 text-amber-700')) }}">{{ $assignment->submission ? ucfirst($assignment->submission->status) : ($assignment->due_at->isPast() ? 'Overdue' : 'Not submitted') }}</span></div>@if($assignment->submission)<div class="mt-4 grid gap-3 text-sm sm:grid-cols-3"><div><p class="text-slate-500">Submitted</p><p class="font-medium text-slate-800">{{ $assignment->submission->submitted_at?->format('d M Y, H:i') ?? '—' }}</p></div><div><p class="text-slate-500">Score</p><p class="font-medium text-slate-800">{{ $assignment->submission->score !== null ? $assignment->submission->score.' / '.$assignment->max_score : 'Pending grading' }}</p></div><div><p class="text-slate-500">Feedback</p><p class="font-medium text-slate-800">{{ $assignment->submission->feedback ?: 'No feedback yet' }}</p></div></div>@endif</article>@empty<div class="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-slate-500">No published assignments for this ward.</div>@endforelse</div><x-pagination :paginator="$assignments" /></div>

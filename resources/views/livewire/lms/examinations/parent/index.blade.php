<div class="space-y-6"><div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">Assessment schedule</p><h2 class="mt-2 text-2xl font-bold text-slate-900">Ward examinations</h2><p class="mt-1 text-sm text-slate-600">Review examination dates for each ward.</p></div><select wire:model.live="studentId" class="block w-full max-w-sm rounded-lg border-slate-300"><option value="">Choose a ward</option>@foreach($students as $student)<option value="{{ $student->id }}">{{ $student->first_name }} {{ $student->last_name }}</option>@endforeach</select><div class="overflow-hidden rounded-2xl border border-slate-200 bg-white"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs font-semibold uppercase text-slate-500"><tr><th class="px-5 py-3">Examination</th><th class="px-5 py-3">Subject</th><th class="px-5 py-3">Term</th><th class="px-5 py-3">Date</th><th class="px-5 py-3">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($examinations as $examination)<tr wire:key="exam-{{ $examination->id }}"><td class="px-5 py-4 font-medium">{{ $examination->title }}</td><td class="px-5 py-4">{{ $examination->classSubject->subject->name }}</td><td class="px-5 py-4">{{ $examination->term->name }}</td><td class="px-5 py-4">{{ $examination->exam_date->format('d M Y') }}</td><td class="px-5 py-4">{{ ucfirst($examination->status) }}</td></tr>@empty<tr><td colspan="5" class="px-5 py-12 text-center text-slate-500">No examinations are scheduled for this ward.</td></tr>@endforelse</tbody></table></div><x-pagination :paginator="$examinations" /></div>

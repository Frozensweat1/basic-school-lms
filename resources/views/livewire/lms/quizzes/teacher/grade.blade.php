<div class="space-y-6"><div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">Quiz grading</p><h2 class="mt-2 text-2xl font-bold text-slate-900">{{ $quiz->title }}</h2></div><div class="grid gap-4">@forelse($attempts as $attempt)<article class="rounded-2xl border border-slate-200 bg-white p-5"><h3 class="font-semibold text-slate-900">{{ $attempt->student->first_name }} {{ $attempt->student->last_name }}</h3><p class="mt-1 text-sm text-slate-600">Status: {{ ucfirst(str_replace('_',' ',$attempt->status)) }}</p><div class="mt-3 flex gap-3"><input wire:model="scores.{{ $attempt->id }}" type="number" min="0" max="{{ $quiz->max_score }}" step=".01" class="rounded-lg border-slate-300"><x-button wire:click="saveGrade({{ $attempt->id }})" icon="save" target="saveGrade({{ $attempt->id }})">Save score</x-button></div></article>@empty<div class="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-slate-500">No quiz attempts yet.</div>@endforelse</div></div>

<div class="space-y-6"><div><p class="text-xs font-semibold uppercase tracking-[.22em] text-slate-500">Assessment work</p><h2 class="mt-2 text-2xl font-bold text-slate-900">My quizzes</h2><p class="mt-1 text-sm text-slate-600">Complete published quizzes within their allowed attempt and time windows.</p></div><div class="grid gap-4">@forelse($quizzes as $quiz)<article wire:key="quiz-{{ $quiz->id }}" class="rounded-2xl border border-slate-200 bg-white p-5"><h3 class="font-semibold text-slate-900">{{ $quiz->title }}</h3><p class="mt-1 text-sm text-slate-500">{{ $quiz->classSubject->subject->name }} · Attempts {{ $quiz->attempt?->attempt_number ?? 0 }}/{{ $quiz->max_attempts }}</p><p class="mt-2 text-sm text-slate-600">{{ $quiz->attempt ? ucfirst(str_replace('_', ' ', $quiz->attempt->status)) : 'Not started' }}@if($quiz->attempt?->score !== null) · Score {{ $quiz->attempt->score }}/{{ $quiz->max_score }}@endif</p>@if($quiz->attempt && $quiz->attempt->status === 'in_progress')<a href="{{ route('lms.quizzes.student.attempt', $quiz->attempt) }}" class="mt-3 inline-flex rounded-xl bg-blue-600 px-4 py-2 text-sm font-semibold text-white">Continue attempt</a>@elseif(!$quiz->attempt || ($quiz->attempt->status === 'completed' && $quiz->attemptCount < $quiz->max_attempts))<x-button wire:click="start({{ $quiz->id }})" icon="play" target="start({{ $quiz->id }})">Start attempt</x-button>@elseif($quiz->attempt->status === 'submitted')<p class="mt-3 text-sm font-medium text-emerald-700">Submitted for grading</p>@endif</article>@empty<div class="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-slate-500">No published quizzes are available.</div>@endforelse</div><x-pagination :paginator="$quizzes" /></div>
